// Logical Operators: && - and, || - or

var fName = "Myhkas";
var isSleepy = true;

if (fName === "Mys" || isSleepy === true) {
  console.log("Get some coffee " + fName);
}

if (fName === "Mys" && isSleepy === true) {
  console.log("Get some coffee " + fName);
}
